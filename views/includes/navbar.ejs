<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <style>
    .navbar-search-wrap {
      flex: 1;
      display: flex;
      justify-content: center;
      padding: 0 1rem;
    }

    .navbar-search {
      display: flex;
      align-items: center;
      width: min(520px, 60vw);
      background: #f5f5f5;
      border: 1px solid #e5e7eb;
      border-radius: 999px;
      padding: 0.55rem 0.9rem;
      gap: 0.6rem;
    }

    .navbar-search i {
      color: #6b7280;
      font-size: 0.9rem;
    }

    .navbar-search input {
      border: none;
      outline: none;
      background: transparent;
      width: 100%;
      font-size: 0.95rem;
      color: #111827;
    }

    .navbar-search input::placeholder {
      color: #6b7280;
    }

    .navbar-search button {
      border: none;
      background: #ff385c;
      color: white;
      border-radius: 999px;
      padding: 0.5rem 1rem;
      font-weight: 600;
      font-size: 0.9rem;
    }

    @media (max-width: 768px) {
      .navbar-search-wrap {
        width: 100%;
        padding: 0.5rem 0 0;
      }

      .navbar-search {
        width: 100%;
      }
    }
  </style>

  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" 
    type="button" 
    data-bs-toggle="collapse" 
    data-bs-target="#navbarNavAltMarkup" >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="navbar-search-wrap">
        <form class="navbar-search" action="/listings" method="GET">
          <i class="fa-solid fa-magnifying-glass"></i>
          <input type="text" name="search" placeholder="Search destinations" aria-label="Search destinations" value="<%= typeof search !== 'undefined' ? search : '' %>" />
          <button type="submit">Search</button>
        </form>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(typeof currUser === 'undefined' || !currUser) { %>
          <a class="nav-link active"  href="/signup"><b>Sign Up</b></a>
          <a class="nav-link" href="/login"><b>Login</b></a>
        <% } else { %>
          <a class="nav-link" href="/logout"><b>Logout</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>